Open Vault — Vanilla demo

How to run

- Open `vanilla/index.html` in a browser (no build tools required).

What it is

- A minimal, client-side demo that mimics adding and listing site/password entries.
- No persistence — refresh clears data.